

<div class="card">
  <h5 class="card-header"><a href="/post/{{id}}">{{title}}</a> Posted by {{user.username}} on {{format_date created_at}}</h5>
  <div class="card-body">
    <h6 class="card-title">{{description}}</h6>
  </div>
</div>